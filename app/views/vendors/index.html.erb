<!DOCTYPE html>
<html>
<head>
  <title>All vendors</title>
</head>

<body>

  <ul>
    <% Vendor.all.each do |vendor| %>
      <li>
        <a href="<%= vendor.path %>" > <%= vendor.name %> </a>
      </li>
    <% end %>
  </ul>
  <a href="/vendors/new">Sign up a new vendor</a>
</body>

</html>
