<!-- <% product = Product.find(params[:id]) %>
<% vendor = Vendor.find(product.vendor_id) %>  -->

<html>
<head>
  <title>All About <%= @product.name %></title>
</head>

<body>
  <h1> <%= @vendor.name %>'s <%= @product.name %> </h1>
  <p> <%= @product.description %> &mdash; $<%= @product.price %></p>
  <a href = <%= @product.edit_path %> >Edit</a>
  <a href = <%= @product.delete_path %> >Delete</a>
  <a href= "/vendors/<%= @product.vendor_id %>">Back to <%= @vendor.name %></a>
</body>
